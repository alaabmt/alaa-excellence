name: Bump Arabic homepage main.js cache

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Arabic homepage cache-busting fix already applied."
